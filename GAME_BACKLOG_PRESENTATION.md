# 🎮 สคริปต์พรีเซนต์: โจทย์ประยุกต์ Game Backlog (Lab 10)
> **อ้างอิงเนื้อหาและทฤษฎีจากใบงาน:** `web231-10-form-crud-route.docx`  
> **ไฟล์เป้าหมายที่ใช้พรีเซนต์:** เส้นทาง `/games` และ `/games/[id]`  
> **คู่มือนี้เขียนขึ้นเพื่อให้คุณเปิดอ่านและพูดตามได้ทันที แม้ไม่มีพื้นฐานมาก่อน**

---

## 📋 สารบัญพรีเซนต์
1. [บทนำเปิดตัว (30 วินาทีแรก)](#1-บทนำเปิดตัว)
2. [ลำดับการ Live Demo หน้าเว็บทีละคลิก พร้อมบทพูด (สำคัญที่สุด)](#2-ลำดับการ-live-demo-หน้าเว็บทีละคลิก)
3. [เจาะลึกโค้ดทีละไฟล์แบบละเอียด (สำหรับเปิดโค้ดให้อาจารย์ดู)](#3-เจาะลึกโค้ดทีละไฟล์)
4. [จุดตาย! คำถามที่อาจารย์ชอบสั่งให้ "แก้โค้ดสดให้ดูหน่อย"](#4-คำถามที่อาจารย์ชอบสั่งให้แก้โค้ดสด)
5. [คลังศัพท์เทคนิคที่ต้องตอบให้อิงตามใบงานที่ 10 (Cheat Sheet)](#5-คลังศัพท์เทคนิคอิงตามใบงาน)

---

## 🎙️ 1. บทนำเปิดตัว
*(ยืนหน้าจอ เปิดเว็บที่หน้าแรก หรือเปิดมาที่เมนู "เกม")*

> **คำพูดแนะนำตัว:**  
> "สวัสดีครับอาจารย์ วันนี้ผมขอพรีเซนต์**โจทย์ประยุกต์ท้ายใบงานที่ 10 เรื่อง Game Backlog** ครับ  
> ในโปรเจกต์นี้ ผมได้นำแนวคิดหลัก 4 เรื่องของใบงานที่ 10 มาพัฒนาเป็นระบบบันทึกเกมที่ตั้งใจจะเล่น ได้แก่:  
> 1. การสร้างฟอร์มแบบ **Controlled Input** และการทำ **Validation** ตรวจสอบความถูกต้อง  
> 2. การจัดการข้อมูลแบบ **Full CRUD** (เพิ่ม ลบ แก้ไข) ผ่าน State ใน React  
> 3. การสร้างหน้ารายละเอียดด้วย **Dynamic Route** (`/games/[id]`) และการตั้งชื่อแท็บด้วย `generateMetadata`  
> 4. และได้ทำ**ส่วนขยายครบทั้ง 4 ข้อ**ตามที่ใบงานแนะนำครับ เดี๋ยวผมขออนุญาตเดโมการทำงานหน้าเว็บให้อาจารย์ดูครับ"

---

## 🖥️ 2. ลำดับการ Live Demo หน้าเว็บทีละคลิก
*(เปิดเบราว์เซอร์ไปที่ `http://localhost:3000/games`)*

### ขั้นที่ 1: อธิบายหน้าตาและข้อมูลภาพรวม
- **สิ่งที่ทำ:** ชี้เมาส์ไปที่ส่วนหัวและแถบสถิติ
- **บทพูด:**  
  *"หน้านี้คือระบบ Game Backlog ครับ มีข้อมูลเริ่มต้น 5 เกมตามที่โจทย์กำหนด โดยแต่ละเกมจะเก็บชื่อเกม, แพลตฟอร์ม, เวลาเล่นโดยประมาณ และสถานะ 3 แบบ คือ ยังไม่เริ่ม, กำลังเล่น, และเล่นจบแล้วครับ"*

---

### ขั้นที่ 2: โชว์ระบบตรวจสอบข้อมูล (Validation) — *อาจารย์ชอบดูจุดนี้มาก*
- **สิ่งที่ทำ:** เลื่อนไปที่ฟอร์มด้านบน **ไม่ต้องกรอกอะไรเลย** แล้วคลิกปุ่ม **"เพิ่มเกม"**
- **ผลลัพธ์ที่โชว์:** จะมีข้อความสีแดงขึ้นเตือน 3 ช่อง:
  1. ใต้ชื่อเกม: *"กรุณาระบุชื่อเกม"*
  2. ใต้แพลตฟอร์ม: *"กรุณาเลือกแพลตฟอร์ม"*
  3. ใต้เวลาเล่น: *"เวลาเล่นต้องเป็นจำนวนเต็มบวก (มากกว่า 0)"*
- **สิ่งที่ทำต่อ:** ลองพิมพ์เวลาเล่นเป็นเลขติดลบ เช่น `-10` หรือเลข `0` หรือทศนิยม `3.5` แล้วกดปุ่มเพิ่มเกม
- **ผลลัพธ์ที่โชว์:** ยังคงมีข้อความเตือนว่าเวลาเล่นต้องเป็นจำนวนเต็มบวก
- **บทพูด:**  
  *"ระบบมีฟังก์ชัน Validation ตรวจสอบก่อนบันทึกครับ ถ้ายังไม่กรอก หรือกรอกเวลาเล่นไม่เป็นจำนวนเต็มบวก ฟอร์มจะไม่ยอมให้บันทึก  โดยผมผูก `aria-invalid` และ `aria-describedby` ตามมาตรฐานการเข้าถึง A11y ของใบงานครับ"*

---

### ขั้นที่ 3: โชว์การเพิ่มเกมใหม่ (Create)
- **สิ่งที่ทำ:** กรอกข้อมูลเกมใหม่จริงๆ เช่น:
  - ชื่อเกม: `Monster Hunter Wilds`
  - แพลตฟอร์ม: เลือก `PC`
  - เวลาเล่นโดยประมาณ: `80`
  - สถานะ: `ยังไม่เริ่ม`
  - บันทึกสั้นๆ: `รอเล่นช่วงต้นปีหน้า`
  - คลิกปุ่ม **"เพิ่มเกม"**
- **ผลลัพธ์ที่โชว์:** การ์ดเกม `Monster Hunter Wilds` จะปรากฏขึ้นมาใหม่ทันทีด้านล่าง และฟอร์มจะล้างค่ากลับเป็นค่าว่าง
- **บทพูด:**  
  *"เมื่อข้อมูลผ่านการตรวจสอบ ฟังก์ชัน `handleCreate` จะใช้ `crypto.randomUUID()` ในการสุ่มสร้าง ID ประจำตัวเกม และเพิ่มเข้าอาเรย์ State แบบ Immutable ด้วย Spread Operator ครับ"*

---และจะแสดงข้อความเตือนใต้ช่องทันที

### ขั้นที่ 4: โชว์การแก้ไขเกม (Update)
- **สิ่งที่ทำ:** เลื่อนลงมาที่เกมที่เพิ่งเพิ่ม (หรือเกมใดก็ได้) แล้วคลิกปุ่มสีส้ม **"แก้ไข"**
- **ผลลัพธ์ที่โชว์:** หน้าจอจะเลื่อนขึ้นไปที่ฟอร์ม ข้อมูลเดิมทั้งหมดจะเด้งกลับเข้าไปในช่องกรอก มีป้ายขึ้นว่า `"กำลังแก้ไขข้อมูล"` และปุ่มเปลี่ยนเป็น `"บันทึกการแก้ไข"` พร้อมปุ่ม `"ยกเลิก"`
- **สิ่งที่ทำต่อ:** ลองเปลี่ยนเวลาเล่นจาก `80` เป็น `100` แล้วกดปุ่ม **"บันทึกการแก้ไข"**
- **ผลลัพธ์ที่โชว์:** ข้อมูลในการ์ดเปลี่ยนเป็น 100 ชั่วโมงทันที และฟอร์มกลับสู่โหมดเพิ่มตามปกติ
- **บทพูด:**  
  *"การแก้ไขจะนำค่าเดิมกลับเข้าฟอร์มผ่านฟังก์ชัน `toGameDraft` และเมื่อกดบันทึก จะใช้เมธอด `.map()` อัปเดตเฉพาะเกมที่มี ID ตรงกันครับ"*

---

### ขั้นที่ 5: โชว์การเปลี่ยนสถานะด่วนจากการ์ด *(ส่วนขยาย ข้อ 1)*
- **สิ่งที่ทำ:** ชี้ไปที่ Dropdown ตรงการ์ดเกมคำว่า "เปลี่ยนสถานะ:"
- **สิ่งที่ทำต่อ:** ลองเปลี่ยนสถานะของเกมจาก `ยังไม่เริ่ม` เป็น `กำลังเล่น`
- **ผลลัพธ์ที่โชว์:** ป้ายสถานะของการ์ดจะเปลี่ยนเป็นสีส้ม "กำลังเล่น" ทันทีโดยไม่ต้องเปิดฟอร์มแก้ไข
- **บทพูด:**  
  *"นี่คือส่วนขยายข้อที่ 1 ครับ ผู้ใช้สามารถคลิกเปลี่ยนสถานะของเกมได้โดยตรงจากการ์ดแต่ละใบได้ทันทีครับ"*

---

### ขั้นที่ 6: โชว์ชั่วโมงดองแบบ Derived State *(ส่วนขยาย ข้อ 2)*
- **สิ่งที่ทำ:** ชี้ให้อาจารย์ดูตัวเลขด้านบนตรงแถบค้นหา: **"ชั่วโมงที่ยังไม่เริ่ม: XX ชม."**
- **สิ่งที่ทำต่อ:** ลองเปลี่ยนสถานะเกมใดเกมหนึ่งจาก "ยังไม่เริ่ม" เป็น "กำลังเล่น" หรือ "เล่นจบแล้ว"
- **ผลลัพธ์ที่โชว์:** ตัวเลขชั่วโมงรวมจะลดลงทันทีแบบ Real-time
- **บทพูด:**  
  *"ตรงนี้คือส่วนขยายข้อที่ 2 ครับ เป็น Derived State ที่คำนวณผลรวมเวลาเล่นเฉพาะเกมที่มีสถานะ 'ยังไม่เริ่ม' สดๆ จาก State โดยไม่ต้องสร้างตัวแปร `useState` ซ้ำซ้อน ช่วยให้ข้อมูลไม่คลาดเคลื่อนครับ"*

---

### ขั้นที่ 7: โชว์การกรองสถานะร่วมกับการค้นหา *(ส่วนขยาย ข้อ 3)*
- **สิ่งที่ทำ:** 
  1. ที่ Dropdown ด้านบน เลือกกรองเป็น **"กำลังเล่น"** (รายการจะเหลือเฉพาะเกมที่กำลังเล่น)
  2. ในช่องค้นหา พิมพ์คำว่า `Elden` หรือ `PC`
- **ผลลัพธ์ที่โชว์:** รายการจะแสดงเฉพาะเกมที่ 'กำลังเล่น' และมีคำค้นหาตรงกัน
- **บทพูด:**  
  *"ส่วนขยายข้อที่ 3 คือตัวกรองสถานะทำงานร่วมกับช่องค้นหาชื่อเกมและแพลตฟอร์มได้พร้อมกันครับ"*

---

### ขั้นที่ 8: โชว์ขั้นตอนยืนยันก่อนลบ (Confirm Delete) *(ส่วนขยาย ข้อ 4)*
- **สิ่งที่ทำ:** คลิกปุ่มสีแดง **"ลบ"** ที่เกมใดเกมหนึ่ง
- **ผลลัพธ์ที่โชว์:** มี Popup Modal สีดำเด้งขึ้นมากลางจอถามว่า:  
  *"ยืนยันการลบเกม: คุณต้องการลบเกม '...' ใช่หรือไม่?"*
- **สิ่งที่ทำต่อ:** 
  1. ลองกด **"ยกเลิก"** ➔ Popup หายไป เกมยังอยู่
  2. กดปุ่ม **"ลบ"** อีกครั้ง แล้วกด **"ลบรายการ"** ➔ เกมจะถูกลบออกจากหน้าจอทันที
- **บทพูด:**  
  *"ส่วนขยายข้อที่ 4 คือขั้นตอนยืนยันก่อนลบครับ ผมใช้ State `deletingId` เก็บไอดีของเกมที่รอยืนยัน เพื่อป้องกันไม่ให้ผู้ใช้เผลอกดลบข้อมูลโดยไม่ได้ตั้งใจครับ"*

---

### ขั้นที่ 9: โชว์หน้ารายละเอียด Dynamic Route (`/games/[id]`)
- **สิ่งที่ทำ:** คลิกที่ **ชื่อเกม** บนการ์ด (เช่น คลิกที่ชื่อ `Elden Ring`)
- **ผลลัพธ์ที่โชว์:**
  1. URL ในเบราว์เซอร์จะเปลี่ยนเป็น `http://localhost:3000/games/elden-ring`
  2. หน้าเว็บจะแสดงรายละเอียดของเกมนั้น
  3. **ชี้ให้อาจารย์ดูที่แท็บเบราว์เซอร์ด้านบนสุด:** ชื่อแท็บจะแสดงเป็น `"Elden Ring | Game Backlog"`
- **บทพูด:**  
  *"เมื่อคลิกชื่อเกม จะเปิดมายังหน้ารายละเอียดแบบ Dynamic Route ผ่านโฟลเดอร์ `[id]` ครับ และผมใช้ฟังก์ชัน `generateMetadata` ดึงชื่อเกมมากำหนดชื่อแท็บบนเบราว์เซอร์ตามข้อมูลจริงครับ"*

---

### ขั้นที่ 10: โชว์หน้า 404 (notFound)
- **สิ่งที่ทำ:** ลองพิมพ์ URL บนเบราว์เซอร์มั่วๆ เช่น `http://localhost:3000/games/game-xyz` แล้วกด Enter
- **ผลลัพธ์ที่โชว์:** จะขึ้นหน้า **404 Not Found** ของ Next.js
- **บทพูด:**  
  *"หากผู้ใช้ระบุรหัสเกมที่ไม่มีอยู่ในระบบ ฟังก์ชัน `notFound()` จะหยุดการทำงานและส่งหน้า 404 ให้ผู้ใช้ทันทีตามข้อกำหนดในใบงานครับ"*

---

## 🔍 3. เจาะลึกโค้ดทีละไฟล์
*(ถ้าอาจารย์บอกว่า "ไหนเปิดโค้ดแต่ละไฟล์ให้ดูซิ")*

### 1. ไฟล์ [types/game.ts](file:///d:/Web-Project/javascript/next-course-hub/types/game.ts)
```ts
export type GameStatus = "not_started" | "playing" | "completed";

export type Game = {
  id: string;
  title: string;
  platform: string;
  estimatedHours: number;
  status: GameStatus;
  description?: string;
};
```
- **จุดที่ต้องอธิบาย:**  
  - กำหนด `GameStatus` เป็น Union Type 3 ค่าตามโจทย์
  - `estimatedHours` กำหนดเป็นชนิด `number` เพื่อคำนวณชั่วโมงรวมได้

---

### 2. ไฟล์ [components/GameForm.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/GameForm.tsx)
- **จุดที่ 1: `GameDraft` (ทำไมต้องแยก Type ระหว่างกรอกออกจาก `Game`)**
  - ในฟอร์มเราเก็บ `estimatedHours: string` เพราะเวลาผู้ใช้พิมพ์ใน input ค่าที่ได้จะเป็น string ก่อน เมื่อผ่านการ validate แล้วค่อยแปลงเป็น number ด้วย `Number()`
- **จุดที่ 2: Computed Property Name ใน `handleChange`**
  ```ts
  function handleChange(event) {
    const { name, value } = event.target;
    setDraft((prev) => ({ ...prev, [name]: value }));
  }
  ```
  - `[name]: value` ทำให้ฟังก์ชันเดียวคุม input ทุกช่องในฟอร์มได้
- **จุดที่ 3: ฟังก์ชัน `validate(draft)`**
  - เช็ก `title.trim() === ""`
  - เช็ก `platform.trim() === ""`
  - เช็ก `!Number.isInteger(hours) || hours <= 0` (จำนวนเต็มบวก)
- **จุดที่ 4: `event.preventDefault()` ใน `handleSubmit`**
  - ป้องกันเบราว์เซอร์รีเฟรชหน้าเว็บเมื่อส่งฟอร์ม

---

### 3. ไฟล์ [components/GameExplorer.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/GameExplorer.tsx)
- **เป็น Parent Component ที่รวม State:**
  - `games`: รายการเกมทั้งหมด (`useState<Game[]>(initialGames)`)
  - `keyword`: คำค้นหา
  - `statusFilter`: ตัวกรองสถานะ
  - `editingId`: รหัสเกมที่กำลังแก้ไข (ถ้าเป็น null = โหมดเพิ่ม)
  - `deletingId`: รหัสเกมที่กำลังรอยืนยันการลบ
- **การทำ CRUD แบบ Immutable Update:**
  - เพิ่มเกม: `setGames((prev) => [...prev, newGame])` โดยใช้ `crypto.randomUUID()` เจนไอดี
  - ลบเกม: `setGames((prev) => prev.filter((g) => g.id !== deletingId))`
  - แก้ไขเกม: `setGames((prev) => prev.map((g) => g.id === id ? { ...updated } : g))`
- **Derived State (คำนวณสด):**
  ```ts
  const unstartedHours = games
    .filter((g) => g.status === "not_started")
    .reduce((sum, g) => sum + g.estimatedHours, 0);
  ```

---

### 4. ไฟล์ [components/GameCard.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/GameCard.tsx)
- เป็น Child Component ที่รับ Props จากแม่:
  - `game: Game`
  - `onEdit`, `onDelete`, `onStatusChange` (Callback Props ที่ส่งเหตการณ์กลับไปหาแม่)
- มีลิงก์ `<Link href={'/games/${game.id}'}>`

---

### 5. ไฟล์ [app/games/[id]/page.tsx](file:///d:/Web-Project/javascript/next-course-hub/app/games/[id]/page.tsx)
- **Dynamic Route Segment:** โฟลเดอร์ชื่อ `[id]`
- **Next.js 15 Requirement:**
  ```ts
  type GameDetailPageProps = {
    params: Promise<{ id: string }>;
  };
  export default async function GameDetailPage({ params }: GameDetailPageProps) {
    const { id } = await params; // ต้อง await params
  ```
- **แสดงหน้า 404:**
  ```ts
  if (!game) {
    notFound();
  }
  ```
- **กำหนดชื่อแท็บตามชื่อเกม:**
  ```ts
  export async function generateMetadata({ params }): Promise<Metadata> {
    const { id } = await params;
    const game = initialGames.find((g) => g.id === id);
    return { title: game ? `${game.title} | Game Backlog` : "ไม่พบเกม" };
  }
  ```

---

## ⚡ 4. คำถามที่อาจารย์ชอบสั่งให้แก้โค้ดสด
*(อาจารย์ทดสอบว่าเราเขียนเองจริงไหม โดยสั่งให้แก้โค้ดสดหน้าห้อง)*

---

### ❓ คำถามที่ 1: "ถ้าอยากเพิ่มช่องกรอก 'คะแนนความชอบ (Rating 1-10)' ในฟอร์ม ต้องแก้ไฟล์ไหนบ้าง?"
**วิธีแก้เรียง 4 สเต็ป:**
1. **[types/game.ts](file:///d:/Web-Project/javascript/next-course-hub/types/game.ts):**
   เพิ่ม `rating?: number;` ใน `type Game`
2. **[components/GameForm.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/GameForm.tsx):**
   - ใน `GameDraft` เพิ่ม `rating: string;`
   - ใน `emptyGameDraft` เพิ่ม `rating: "",`
   - ใน `toGameDraft` เพิ่ม `rating: game.rating ? String(game.rating) : "",`
   - ใน JSX เพิ่ม `<input name="rating" type="number" min="1" max="10" value={draft.rating} onChange={handleChange} />`
3. **[components/GameExplorer.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/GameExplorer.tsx):**
   - ใน `handleCreate` เพิ่ม `rating: Number(draft.rating),`
   - ใน `handleUpdate` เพิ่ม `rating: Number(draft.rating),`
4. **[components/GameCard.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/GameCard.tsx):**
   - เพิ่มบรรทัดแสดงผล `<p>คะแนน: {game.rating}/10</p>`

---

### ❓ คำถามที่ 2: "ถ้าอาจารย์อยากเปลี่ยนเงื่อนไขชั่วโมงเล่น จากเดิมแค่มากกว่า 0 ให้กลายเป็น 'ต้องอยู่ระหว่าง 1 ถึง 300 ชั่วโมง' แก้ตรงไหน?"
**วิธีแก้:** เปิดไฟล์ **[components/GameForm.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/GameForm.tsx)** ที่เดียวเลย!
มองหาฟังก์ชัน `validate` แล้วเปลี่ยนเงื่อนไข:
```ts
// เดิม: if (!Number.isInteger(hours) || hours <= 0)
// เปลี่ยนเป็น:
if (!Number.isInteger(hours) || hours < 1 || hours > 300) {
  nextErrors.estimatedHours = "เวลาเล่นต้องเป็นจำนวนเต็มระหว่าง 1 ถึง 300 ชั่วโมง";
}
```

---

### ❓ คำถามที่ 3: "ถ้าอยากให้คำนวณจำนวนชั่วโมงของเกมที่ 'เล่นจบแล้ว (completed)' แสดงเพิ่มอีกจุดหนึ่ง ทำยังไง?"
**วิธีแก้:** เปิดไฟล์ **[components/GameExplorer.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/GameExplorer.tsx)**  
เพิ่ม Derived State อีก 1 บรรทัดใต้ `unstartedHours`:
```ts
const completedHours = games
  .filter((g) => g.status === "completed")
  .reduce((sum, g) => sum + g.estimatedHours, 0);
```
แล้วนำ `{completedHours}` ไปแสดงผลใน JSX ได้ทันที

---

### ❓ คำถามที่ 4: "ทำไมเกมที่เพิ่งกดเพิ่มใหม่ในหน้าเว็บ พอกดเข้าไปดูรายละเอียดถึงขึ้น 404 ไม่พบข้อมูล?" *(คำถามทฤษฎีเด็ดจากกิจกรรม 5.1 ในใบงาน)*
**คำตอบที่ต้องตอบ:**  
*"เพราะว่าหน้ารายละเอียด (`/games/[id]`) เป็น Server Component ที่อ่านข้อมูลจากไฟล์ `data/games.ts` บนฝั่งเซิร์ฟเวอร์ครับ แต่เกมที่เราเพิ่งเพิ่มใหม่ มันถูกบันทึกอยู่ใน **State ของเบราว์เซอร์ (Client State)** เท่านั้น ยังไม่ได้ถูกส่งไปเซฟลงฐานข้อมูลหรือไฟล์จริง เมื่อหน้ารายละเอียดไปค้นหาใน `data/games.ts` จึงหาไม่เจอและแสดงผลเป็น 404 ซึ่งเป็นข้อจำกัดของการเก็บข้อมูลไว้ใน State ของหน้าเดียวตามที่ใบงานระบุไว้ครับ"*

---

### ❓ คำถามที่ 5: "ทำไม `GameCard.tsx` ถึงไม่ต้องใส่ 'use client' อยู่บนสุด แต่ก็กดปุ่มและมี Dropdown ได้?"
**คำตอบที่ต้องตอบ:**  
*"เพราะว่าไฟล์ `GameCard.tsx` ถูก import เข้าไปใช้งานใน `GameExplorer.tsx` ซึ่งมีการประกาศ `'use client'` ไว้อยู่แล้วครับ ใน Next.js App Router คอมโพเนนต์ลูกใดก็ตามที่อยู่ใต้เส้นแบ่ง Client Component จะกลายเป็น Client Component ตามไปด้วยโดยอัตโนมัติครับ"*

---

## 📚 5. คลังศัพท์เทคนิคอิงตามใบงาน

| คำศัพท์ในใบงาน | ความหมายที่ต้องตอบอาจารย์ |
| :--- | :--- |
| **Controlled Input** | อินพุตที่ค่าของช่องถูกผูกไว้กับ State ผ่าน `value` และอัปเดตกลับเข้า State ผ่าน `onChange` ทำให้ State เป็นแหล่งข้อมูลจริงเพียงแหล่งเดียว |
| **Computed Property Name** | การใช้ชื่อตัวแปรเป็น Key ของ Object ขณะทำงาน เช่น `[name]: value` ทำให้เขียนฟังก์ชัน `handleChange` ฟังก์ชันเดียวรองรับได้ทุกช่องในฟอร์ม |
| **FormEvent & preventDefault** | `FormEvent` คือชนิดของเหตุการณ์ส่งฟอร์ม และ `event.preventDefault()` ใช้ยับยั้งพฤติกรรมเริ่มต้นของเบราว์เซอร์ เพื่อไม่ให้หน้าเว็บรีโหลดใหม่ |
| **Lifting State Up** | การย้าย State ของรายการเกมขึ้นไปไว้ที่ Parent (`GameExplorer`) เพื่อให้ฟอร์มและรายการการ์ดใช้ข้อมูลชุดเดียวกันได้ |
| **Callback Props** | Props ที่เป็นฟังก์ชันที่แม่ส่งให้ลูก (เช่น `onEdit`, `onDelete`) เพื่อให้ลูกเรียกแจ้งเหตุการณ์กลับขึ้นมาหาแม่ |
| **Derived State** | ข้อมูลที่คำนวณสดจาก State ที่มีอยู่แล้ว (เช่น `unstartedHours` หรือ `visibleGames`) โดยไม่ต้องสร้าง State ซ้ำซ้อน |
| **Immutable Update** | การอัปเดต State โดยสร้าง Object หรือ Array ก้อนใหม่เสมอ ห้ามแก้ไขค่าเดิมตรงๆ เช่น ใช้ Spread `[...prev, new]` หรือ `.filter()` หรือ `.map()` |
| **Dynamic Route (`[id]`)** | เส้นทาง URL ที่มีค่าพารามิเตอร์เปลี่ยนไปตามแต่ละไอเทม โดยสร้างโฟลเดอร์ในวงเล็บเหลี่ยม |
| **Promise params & `await`** | ตั้งแต่ Next.js 15 พารามิเตอร์ `params` เป็น Promise จึงต้องใส่ `await params` ในฟังก์ชัน `async` ก่อนดึงค่า `id` มาใช้ |
| **`notFound()`** | ฟังก์ชันของ Next.js ที่สั่งหยุดการทำงานของหน้านั้น แล้วแสดงหน้า 404 Not Found ทันที |
| **`generateMetadata`** | ฟังก์ชันฝั่ง Server ของ Next.js ที่คำนวณและตั้งชื่อหัวแท็บบนเบราว์เซอร์ตามข้อมูลจริงของหน้านั้น |

---

> 💡 **ข้อแนะนำสุดท้าย:**  
> ก่อนเข้าพรีเซนต์ ให้เปิดหน้าเว็บ `http://localhost:3000/games` ทิ้งไว้ แล้วซ้อมคลิกตาม **หัวข้อที่ 2** สัก 1 รอบ รับรองว่าอาจารย์ถามอะไรมา คุณจะตอบได้อย่างแม่นยำและได้คะแนนเต็มแน่นอนครับ!
