# 🎓 สคริปต์พรีเซนต์งาน Lab 10: Form, Validation, CRUD และ Dynamic Route
> **ไฟล์นี้จัดทำขึ้นพิเศษสำหรับใช้เปิดอ่านขณะพรีเซนต์ให้อาจารย์**  
> (อ่านง่าย พูดตามได้ทันที แม้ความรู้พื้นฐานเป็นศูนย์ พร้อมเฉลยคำถามเจาะลึกที่อาจารย์ชอบสั่งแก้โค้ดสด)

---

## 📌 สารบัญสำหรับเปิดดูตอนพรีเซนต์
1. [ขั้นตอนการเปิดเดโมหน้าเว็บ (ทำตามลำดับนี้อาจารย์จะประทับใจมาก)](#-1-ขั้นตอนการเดโมหน้าเว็บ-live-demo)
2. [สคริปต์คำพูดพรีเซนต์ทีละหน้า (พูดตามนี้ได้เลย)](#-2-สคริปต์คำพูดพรีเซนต์)
3. [สรุปโครงสร้างโค้ดแบบเข้าใจง่าย (ถ้าอาจารย์ให้เปิดโค้ดดู)](#-3-อธิบายโค้ดแต่ละไฟล์แบบสั้นกระชับ)
4. [จุดตาย! คำถามที่อาจารย์ชอบสั่งให้ "ลองแก้โค้ดสดให้ดูหน่อย"](#-4-โจทย์สั่งแก้โค้ดสด-ที่อาจารย์มักจะถาม)
5. [คำศัพท์สำคัญที่อาจารย์ชอบถามความหมาย (Cheat Sheet)](#-5-คลังคำตอบ-ศัพท์เทคนิคที่อาจารย์ชอบจี้ถาม)

---

## 🖥️ 1. ขั้นตอนการเดโมหน้าเว็บ (Live Demo)

ก่อนเริ่มพูด ให้รันเว็บไว้ที่ `http://localhost:3000` แล้วทำตาม 2 พาร์ตนี้:

### พาร์ตที่ 1: หน้าจัดการรายวิชา (`/courses`)
1. **โชว์การดักจับข้อผิดพลาด (Validation):**
   - ไม่ต้องพิมพ์อะไรในฟอร์มเลย ให้กดปุ่ม **"บันทึกข้อมูล"** ทันที
   - **สิ่งที่เกิดขึ้น:** จะมีข้อความสีแดงเตือนใต้ช่องรหัสวิชา, ชื่อวิชา, หน่วยกิต, และผู้สอน
   - *คำพูด:* "ระบบจะตรวจสอบข้อมูลก่อนบันทึกครับ ถ้ายังไม่กรอกหรือกรอกไม่ครบ ฟอร์มจะไม่ยอมให้ส่งข้อมูล"
2. **โชว์การดักหน่วยกิตผิดเงื่อนไข:**
   - ลองกรอกหน่วยกิตเป็น `0` หรือ `7` หรือ `2.5` แล้วกดบันทึก
   - **สิ่งที่เกิดขึ้น:** มีข้อความเตือนว่า "หน่วยกิตต้องเป็นจำนวนเต็มตั้งแต่ 1 ถึง 6"
3. **โชว์การเพิ่มวิชาใหม่ (Create):**
   - กรอกข้อมูลวิชาใหม่ เช่น:
     - รหัสวิชา: `CS302`
     - หน่วยกิต: `3`
     - ชื่อวิชา: `Mobile App Development`
     - อาจารย์ผู้สอน: `ดร.พัฒนา แอปพลิเคชัน`
   - กดปุ่ม **"บันทึกข้อมูล"**
   - **สิ่งที่เกิดขึ้น:** การ์ดวิชาใหม่จะโผล่มาต่อท้ายด้านล่างทันที และช่องในฟอร์มจะถูกล้างค่าว่างพร้อมให้กรอกวิชาถัดไป
4. **โชว์การแก้ไขวิชา (Update):**
   - เลื่อนลงมาที่วิชาที่เพิ่งเพิ่ม (หรือวิชาใดก็ได้) แล้วคลิกปุ่มสีส้ม **"แก้ไข"**
   - **สิ่งที่เกิดขึ้น:** หน้าจอจะเลื่อนขึ้นไปที่ฟอร์ม และข้อมูลเดิมทั้งหมดจะวิ่งกลับเข้าไปอยู่ในช่องกรอก พร้อมมีป้ายเตือน "กำลังแก้ไขข้อมูล" และมีปุ่ม "ยกเลิก" ปรากฏขึ้นมา
   - ลองเปลี่ยนชื่อวิชา หรือเปลี่ยนหน่วยกิต แล้วกดปุ่ม **"บันทึกการแก้ไข"**
   - **สิ่งที่เกิดขึ้น:** การ์ดวิชานั้นจะอัปเดตข้อมูลใหม่ทันที
5. **โชว์การลบวิชา (Delete):**
   - คลิกปุ่มสีแดง **"ลบ"** ที่การ์ดวิชาใดก็ได้
   - **สิ่งที่เกิดขึ้น:** การ์ดวิชานั้นจะหายไปจากหน้าจอทันที และตัวเลขนับจำนวนวิชาจะลดลง
6. **โชว์ช่องค้นหา (Search Filter):**
   - พิมพ์ชื่อวิชาหรือรหัสวิชาในช่องค้นหา เช่น พิมพ์ `CS101` หรือ `Data`
   - **สิ่งที่เกิดขึ้น:** รายการจะถูกกรองสดทันทีขณะพิมพ์
7. **โชว์หน้ารายละเอียด (Dynamic Route):**
   - คลิกที่ **ชื่อวิชา** (เช่นคลิก `Introduction to Computer Science`)
   - **สิ่งที่เกิดขึ้น:** เว็บจะเปลี่ยน URL ไปที่ `/courses/CS101` และเปิดหน้ารายละเอียดของวิชานั้น ชี้ให้อาจารย์ดูที่แท็บบนสุดของเบราว์เซอร์ว่าชื่อแท็บเปลี่ยนตามชื่อวิชาจริง

---

### พาร์ตที่ 2: หน้า Game Backlog (`/games`) (โจทย์ประยุกต์ท้ายเล่ม)
คลิกเมนู **"เกม"** ที่ Navbar ด้านบน:
1. **โชว์ความสามารถ CRUD ของเกม:**
   - ชี้ให้อาจารย์ดูว่ามีฟอร์มเพิ่มเกม ตรวจสอบชื่อเกม, แพลตฟอร์ม, และเวลาเล่น (> 0)
2. **โชว์การเปลี่ยนสถานะด่วน (ส่วนขยาย):**
   - ที่การ์ดเกมแต่ละใบ สามารถกดเปลี่ยน Dropdown จาก "ยังไม่เริ่ม" ➔ "กำลังเล่น" ➔ "เล่นจบแล้ว" ได้ทันทีโดยไม่ต้องเปิดฟอร์มแก้ไข
3. **โชว์จำนวนชั่วโมงดอง (Derived State):**
   - ชี้ให้ดูสถิติด้านบนตรงข้อความ **"ชั่วโมงที่ยังไม่เริ่ม: XX ชม."**
   - พอลองเปลี่ยนสถานะเกมจาก "ยังไม่เริ่ม" เป็น "กำลังเล่น" ตัวเลขชั่วโมงดองจะลดลงทันทีแบบอัตโนมัติ
4. **โชว์การยืนยันก่อนลบ (Confirm Delete):**
   - พอกดปุ่ม "ลบ" ที่เกม จะมี Popup เด้งขึ้นมาถามเพื่อยืนยันก่อนลบ ป้องกันการกดพลาด
5. **โชว์หน้ารายละเอียดเกม:**
   - คลิกที่ชื่อเกม จะเปิดไปที่ `/games/elden-ring` ตาม Dynamic Route

---

## 🗣️ 2. สคริปต์คำพูดพรีเซนต์

> 💡 **แนะนำให้พูดตามลำดับนี้ ใช้เวลาประมาณ 3-5 นาที:**

"สวัสดีครับอาจารย์ วันนี้ผมขอพรีเซนต์งานปฏิบัติการที่ 10 เรื่อง **Form, Validation, CRUD และ Dynamic Route** ครับ

งานในครั้งนี้ต่อยอดจากครั้งก่อน โดยเปลี่ยนจากเว็บที่แสดงผลอย่างเดียว ให้กลายเป็นเว็บที่ผู้ใช้สามารถ **เพิ่ม แก้ไข ลบ (CRUD)** ข้อมูลได้จริง และมีหน้ารายละเอียดแบบ **Dynamic Route** ครับ

**ในส่วนแรก คือ หน้ารายวิชา (`/courses`):**
1. ผมได้สร้างฟอร์มแบบ **Controlled Input** โดยรวม State ของทุกช่องไว้ในก้อนเดียวชื่อ `draft` และใช้ฟังก์ชัน `handleChange` ฟังก์ชันเดียวคุมทุกช่องด้วยเทคนิค **Computed Property Name** ครับ
2. มีฟังก์ชัน **Validation** ตรวจสอบว่าต้องกรอกรหัสวิชา, ชื่อวิชา, และหน่วยกิตต้องเป็นจำนวนเต็ม 1 ถึง 6 ถ้าไม่ผ่านจะมีข้อความสีแดงเตือนใต้ช่องนั้นทันที
3. รายการวิชาถูกเก็บอยู่ใน State ของ `CourseExplorer` ทำให้ทำ CRUD ได้แบบ **Immutable Update** คือ:
   - เพิ่มวิชาใหม่โดยใช้ `crypto.randomUUID()` เจนไอดี แล้วใช้ Spread Operator `[...prev, newCourse]`
   - แก้ไขวิชาโดยใช้ `.map()`
   - ลบวิชาโดยใช้ `.filter()`
4. และเมื่อคลิกที่ชื่อวิชา จะเปิดไปที่หน้ารายละเอียดแบบ **Dynamic Route** ผ่านเส้นทาง `/courses/[id]` ซึ่งใน Next.js 15 ต้องใช้คำสั่ง `await params` เพื่ออ่านรหัสวิชา และมีการใช้ `notFound()` แสดงหน้า 404 กรณีค้นหาไม่เจอ รวมถึงใช้ `generateMetadata` เปลี่ยนชื่อแท็บตามชื่อวิชาจริงครับ

**ในส่วนที่สอง คือ โจทย์ประยุกต์ Game Backlog (`/games`):**
- ผมได้นำแนวคิดทั้งหมดมาสร้างระบบบันทึกเกมที่ตั้งใจจะเล่น มีฟอร์มตรวจสอบข้อมูล แพลตฟอร์ม และชั่วโมงเล่น
- และได้ทำ**ส่วนขยายครบทุกข้อ** คือ:
  1. มีตัวเลือกเปลี่ยนสถานะเกมได้โดยตรงจากการ์ด
  2. คำนวณชั่วโมงของเกมที่ยังไม่เริ่มเล่นแบบ Derived State อัปเดตสดอัตโนมัติ
  3. กรองตามสถานะและค้นหาชื่อเกมได้พร้อมกัน
  4. มีหน้าต่าง Popup ยืนยันก่อนลบเพื่อความปลอดภัยครับ"

---

## 📂 3. อธิบายโค้ดแต่ละไฟล์แบบสั้นกระชับ

หากอาจารย์บอกว่า *"ไหนเปิดโค้ดให้ดูหน่อยว่าเขียนยังไง"* ให้เปิดไฟล์ตามนี้:

### 1. ไฟล์ [components/CourseForm.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/CourseForm.tsx)
- **หน้าที่:** ฟอร์มสำหรับกรอกข้อมูล
- **จุดสำคัญในโค้ด:**
  - `CourseDraft`: Type ที่ทุกช่องเป็น `string` เพื่อให้เก็บสิ่งที่ผู้ใช้พิมพ์จริงได้ก่อนแปลง
  - `handleChange`: มีบรรทัด `[name]: value` เรียกว่า **Computed Property Name** ช่วยให้ไม่ต้องเขียนฟังก์ชันแยก 4 ช่อง
  - `validate()`: ตรวจสอบความถูกต้อง ส่งคืนเป็น object `errors`
  - `handleSubmit()`: มี `event.preventDefault()` เพื่อไม่ให้หน้าเว็บรีเฟรชตอนกดส่งฟอร์ม

### 2. ไฟล์ [components/CourseExplorer.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/CourseExplorer.tsx)
- **หน้าที่:** เป็นคอมโพเนนต์แม่ (Parent) ที่คุม State ทั้งหมด
- **จุดสำคัญในโค้ด:**
  - `const [courses, setCourses] = useState(initialCourses)`: เก็บรายการวิชาไว้ใน State เพื่อให้เพิ่ม/ลบ/แก้ได้
  - `const [editingId, setEditingId] = useState(null)`: จำว่ากำลังแก้วิชาไหนอยู่ ถ้าเป็น `null` แปลว่าโหมดเพิ่มใหม่
  - `handleCreate()`: ใช้ `crypto.randomUUID()` สุ่มรหัสไอดีที่ไม่ซ้ำกัน
  - `handleDelete()`: ใช้ `.filter(course => course.id !== id)`
  - `handleUpdate()`: ใช้ `.map(...)`
  - `<CourseForm key={editingId ?? "new"} ... />`: ใส่ `key` เพื่อบังคับให้ฟอร์มรีเซ็ตค่าทันทีที่สลับวิชาที่แก้ไข

### 3. ไฟล์ [components/CourseCard.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/CourseCard.tsx)
- **หน้าที่:** การ์ดแสดงผล 1 วิชา
- **จุดสำคัญในโค้ด:**
  - รับ `onEdit` และ `onDelete` มาจากแม่ (เรียกว่า **Callback Props**) พอกดปุ่มจะส่งสัญญาณกลับไปบอกแม่ให้แก้ไขหรือลบ
  - มี `<Link href={'/courses/${course.id}'}>` พาไปหน้ารายละเอียด

### 4. ไฟล์ [app/courses/[id]/page.tsx](file:///d:/Web-Project/javascript/next-course-hub/app/courses/[id]/page.tsx)
- **หน้าที่:** หน้ารายละเอียดวิชา (Dynamic Route)
- **จุดสำคัญในโค้ด:**
  - โฟลเดอร์ชื่อ `[id]` หมายถึงค่าอะไรก็ได้ที่พิมพ์ใน URL เช่น `/courses/CS101`
  - `const { id } = await params;`: ใน Next.js 15 `params` เป็น Promise จึงต้องใส่ `await`
  - `if (!course) notFound();`: ถ้าหาไม่เจอ ให้แสดงหน้า 404
  - `generateMetadata()`: ฟังก์ชันของ Next.js ที่ใช้เปลี่ยน `<title>` บนแท็บตามชื่อวิชา

---

## ⚡ 4. โจทย์สั่งแก้โค้ดสด (ที่อาจารย์มักจะถาม)

อาจารย์มักจะทดสอบว่าเราเขียนเองหรือจ้างทำ โดยสั่งให้แก้โค้ดสดๆ ให้จำวิธีแก้ตามสถานการณ์เหล่านี้ไว้:

---

### 🔥 คำถามที่ 1: "ถ้าอยากเพิ่มช่องกรอก 'ห้องเรียน (room)' ในฟอร์ม ต้องแก้ตรงไหนบ้าง?"
**คำตอบ & วิธีแก้ 4 สเต็ปง่ายๆ:**
1. **เปิด [types/course.ts](file:///d:/Web-Project/javascript/next-course-hub/types/course.ts):**  
   เพิ่ม `room?: string;` เข้าไปใน `type Course`
2. **เปิด [components/CourseForm.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/CourseForm.tsx):**  
   - ใน `CourseDraft` เพิ่ม `room: string;`
   - ใน `emptyDraft` เพิ่ม `room: "",`
   - ใน `toDraft()` เพิ่ม `room: course.room || "",`
   - ใน JSX ก๊อปปี้บล็อก `<input>` มา 1 ช่อง เปลี่ยน `id="room"` `name="room"` `value={draft.room}`
3. **เปิด [components/CourseExplorer.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/CourseExplorer.tsx):**  
   - ใน `handleCreate` เพิ่ม `room: draft.room.trim(),`
   - ใน `handleUpdate` เพิ่ม `room: draft.room.trim(),`
4. **เปิด [components/CourseCard.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/CourseCard.tsx):**  
   เพิ่มบรรทัดแสดงผล `<p>ห้องเรียน: {course.room}</p>`

---

### 🔥 คำถามที่ 2: "ถ้าอยากเปลี่ยนเงื่อนไขหน่วยกิต ให้รับได้ตั้งแต่ 1 ถึง 9 หน่วยกิต ต้องแก้ตรงไหน?"
**วิธีแก้:** เปิดไฟล์ [components/CourseForm.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/CourseForm.tsx) จุดเดียวเลย!
1. เลื่อนไปที่ฟังก์ชัน `validate`:
   ```ts
   // แก้จาก creditNum > 6 เป็น 9
   if (!Number.isInteger(creditNum) || creditNum < 1 || creditNum > 9) {
     nextErrors.credit = "หน่วยกิตต้องเป็นจำนวนเต็มตั้งแต่ 1 ถึง 9";
   }
   ```
2. เลื่อนลงมาที่แท็ก `<input name="credit">` แก้แอตทริบิวต์ `max="9"`

---

### 🔥 คำถามที่ 3: "ทำไมพอกดเพิ่มวิชาใหม่ในหน้าเว็บ แล้วพอกดคลิกเข้าไปดูรายละเอียด ถึงขึ้น 404 ไม่พบรายวิชา?"
*(คำถามนี้มีอยู่ในกิจกรรม 5.1 ของใบงาน อาจารย์ชอบถามมาก!)*  
**คำตอบที่ถูกต้อง:**  
"เพราะว่าหน้ารายละเอียด (`/courses/[id]`) เป็น Server Component ที่อ่านข้อมูลจากไฟล์ `data/courses.ts` ครับ แต่วิชาที่เราเพิ่งกดเพิ่มเข้าไป มันถูกบันทึกอยู่ใน **State ของเบราว์เซอร์ (Client Component)** เท่านั้น ยังไม่ได้ถูกบันทึกลงฐานข้อมูลจริง เมื่อหน้ารายละเอียดไปค้นหาในไฟล์ `data/courses.ts` จึงหาไม่เจอและแสดงหน้า 404 ซึ่งเป็นข้อจำกัดของการเก็บข้อมูลไว้ใน State หน้าเดียวครับ"

---

### 🔥 คำถามที่ 4: "ถ้าอยากให้ช่องค้นหา สามารถค้นหาจาก 'หน่วยกิต' ได้ด้วย ต้องแก้ตรงไหน?"
**วิธีแก้:** เปิดไฟล์ [components/CourseExplorer.tsx](file:///d:/Web-Project/javascript/next-course-hub/components/CourseExplorer.tsx)  
มองหาตัวแปร `visibleCourses` แล้วเพิ่มเงื่อนไข:
```ts
const visibleCourses = courses.filter((course) => {
  return (
    course.name.toLowerCase().includes(searchText) ||
    course.code.toLowerCase().includes(searchText) ||
    course.instructor.toLowerCase().includes(searchText) ||
    course.credit.toString().includes(searchText) // << เพิ่มบรรทัดนี้
  );
});
```

---

### 🔥 คำถามที่ 5: "ถ้าอยากให้อาจารย์กดลบวิชาแล้ว มีกล่องถามยืนยันแบบหน้าเกม ทำยังไง?"
**วิธีแก้:**  
บอกอาจารย์ว่า "เราสามารถสร้าง State เพิ่มใน `CourseExplorer` ชื่อ `const [deletingId, setDeletingId] = useState<string | null>(null)` แบบเดียวกับที่ผมทำไว้ในหน้า `GameExplorer` ครับ เมื่อผู้ใช้กดปุ่มลบ จะเซ็ต ID ไว้ แล้วเปิดหน้าต่าง Popup ยืนยันก่อนสั่ง `.filter()` ออกจากอาเรย์ครับ"

---

## 📖 5. คลังคำตอบ: ศัพท์เทคนิคที่อาจารย์ชอบจี้ถาม

| คำศัพท์ | คำตอบที่ต้องตอบอาจารย์ (จำสั้นๆ พอ) |
| :--- | :--- |
| **Controlled Input** | อินพุตที่ค่าของช่องกรอกถูกควบคุมด้วย **State** ผ่านคุณสมบัติ `value` และอัปเดตกลับเข้า State ผ่าน `onChange` |
| **Computed Property Name** | การใช้ชื่อตัวแปรเป็น Key ของ Object ด้วยเครื่องหมายวงเล็บเหลี่ยม เช่น `[name]: value` ทำให้เขียนฟังก์ชันเดียวคุมได้ทุกช่อง |
| **Lifting State Up** | การย้าย State จากลูกขึ้นไปไว้ที่คอมโพเนนต์แม่ (Parent) เพื่อให้หลายๆ ส่วนสามารถใช้ข้อมูลชุดเดียวกันได้ |
| **Callback Props** | Props ที่เป็นฟังก์ชันที่แม่ส่งให้ลูก เพื่อให้ลูกเรียกใช้เมื่อเกิดเหตุการณ์ เช่น `onEdit`, `onDelete` |
| **Derived State** | ข้อมูลที่คำนวณสดจาก State ที่มีอยู่แล้ว เช่น `visibleCourses` หรือผลรวมชั่วโมง โดยไม่ต้องสร้าง `useState` เพิ่ม |
| **Immutable Update** | การอัปเดตข้อมูลใน State โดยสร้างค่าใหม่เสมอ ห้ามแก้ไขค่าเดิมตรงๆ เช่น ใช้ Spread `[...prev, new]` แทน `.push()` |
| **Dynamic Route** | เส้นทาง URL ที่มีส่วนที่เปลี่ยนไปตามพารามิเตอร์ โดยสร้างโฟลเดอร์ในวงเล็บเหลี่ยม เช่น `[id]` |
| **`crypto.randomUUID()`** | ฟังก์ชันมาตรฐานของเบราว์เซอร์สำหรับสุ่มสร้าง Unique ID ไม่ซ้ำกันในรูปแบบ UUID |
| **`notFound()`** | ฟังก์ชันของ Next.js ที่สั่งให้หยุดเรนเดอร์หน้านั้น แล้วแสดงหน้า 404 (Not Found) ทันที |
| **`generateMetadata`** | ฟังก์ชันฝั่ง Server ของ Next.js ที่ใช้คำนวณและตั้งชื่อหัวแท็บเบราว์เซอร์ (`<title>`) ตามข้อมูลจริง |

---

> 🎯 **คำแนะนำก่อนเข้าห้องพรีเซนต์:**  
> ลองเปิดรัน `npm run dev` แล้วเปิดเว็บ `http://localhost:3000` ซ้อมคลิกตาม **หัวข้อที่ 1** สัก 1-2 รอบ รับรองว่าพรีเซนต์ได้อย่างมั่นใจแน่นอนครับ!
